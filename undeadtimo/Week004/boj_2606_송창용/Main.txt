너비 우선 탐색 문제.

노드와 노드의 연결을 담아줄
2차원 배열과...
아니지 class를 만들까?
class를 만드는게 더 정석일 것 같은데.

Node class를 따로 만드는 것으로 하자.

boolean 배열을 만드는 방법밖에 없네.

노드의 수(컴퓨터의 개수) + 1 크기의 boolean 배열을 생성해줘서.
인덱스로 해당 컴퓨터가 방문이 되었는지 방문이 되지 않았는지를 확인하자.

1. Node class 만들기.
어떻게?
1) 데이터 값을 담아줄 
연결된 노드가 몇 개 있을줄 알고?
하지말자.

1. 2차원 배열을 만드는데, 두 번째 배열의 깊이에는 list가 들어가도록,

어차피 탐색 여부는 boolean 배열을 통해서 판단할 것이니까,
이전에 생각했던 0이나 -1값으로 변환해서 탐색 여부를 판단하지 않아도 되어,
list 형태로 구현해도 상관없다.

1. 2차원 배열 list 만들기.

2. 값 받아와서 list에 저장하기.

3. 2중 for문 만들기.
어떻게?
첫 번째는 모든 노드들을 차례대로 탐색하도록 만들고,
두 번째는 각 노드 별로 이어져있는 노드를 담고 있는 list를 전부 순회하도록 만든다, size로 범위 지정해주면 될듯.
로직은, 다른 거 필요 없이, boolean값이 기본값이 false니까 탐색이 되었다면, true를 아니면 false인 것으로 해서,
cnt 변수를 하나씩 증가시켜주기만 하면 될듯하다.
